import styled from 'styled-components';

type Props = {
  coinId: string;
};

const BasicInfo = ({ coinId }: Props) => {
  return <Wrapper>BASIC</Wrapper>;
};

const Wrapper = styled.div``;

export default BasicInfo;
